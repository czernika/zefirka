{
  "name": "zefirka",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "packageManager": "bun@1.2.18",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
